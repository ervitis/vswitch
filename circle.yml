machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - pip install -r requirements_environment.txt
    - pip install -r requirements_tests.txt

  override:
    - pyenv local 2.7.10 3.4.0 3.5.0